26736719
Syntax error in updated 0.12 react docs?
<p>When I try and run the <a href="http://facebook.github.io/react/docs/transferring-props.html#transferring-with-...-in-jsx" rel="noreferrer">Transferring with ... in JSX</a> example</p>&#xA;&#xA;<pre><code>var FancyCheckbox = React.createClass({&#xA;  render: function() {&#xA;    var { checked, ...other } = this.props;&#xA;    var fancyClass = checked ? 'FancyChecked' : 'FancyUnchecked';&#xA;    // `other` contains { onClick: console.log } but not the checked property&#xA;    return (&#xA;      &lt;div {...other} className={fancyClass} /&gt;&#xA;    );&#xA;  }&#xA;});&#xA;React.render(&#xA;  &lt;FancyCheckbox checked={true} onClick={console.log}&gt;&#xA;    Hello world!&#xA;  &lt;/FancyCheckbox&gt;,&#xA;  document.body&#xA;);&#xA;</code></pre>&#xA;&#xA;<p>I get <code>Uncaught SyntaxError: Unexpected token {</code>. Same when I try to run it in <a href="http://jsfiddle.net/ke4bmqny/1/" rel="noreferrer">jsfiddle</a></p>&#xA;&#xA;<p>Is this an error or is some extra code transpilation required for this to work?</p>&#xA;
<p>To use this feature you have to have <a href="http://facebook.github.io/react/blog/2014/03/21/react-v0.10.html#react-tools" rel="noreferrer">harmony enabled</a>. Unfortunately the fiddle integration you're using is not having it enabled.</p>&#xA;&#xA;<p>There is a <a href="https://github.com/facebook/react/commit/0f5368341ff96c88c3fbef57dfa089b43d129a06" rel="noreferrer">commit made recently</a> which adds it, so very likely it will be up there. </p>&#xA;&#xA;<p>If you want to use it right away, you can either host the file somewhere, or just add the script directly to the fiddle:</p>&#xA;&#xA;<pre><code>&lt;script&gt;&#xA;  (function () {&#xA;    var tag = document.querySelector(&#xA;            'script[type="application/javascript;version=1.7"]'&#xA;    );&#xA;    if (!tag || tag.textContent.indexOf('window.onload=function(){') !== -1) {&#xA;      alert('Bad JSFiddle configuration, please fork the original React JSFiddle');&#xA;    }&#xA;    tag.setAttribute('type', 'text/jsx;harmony=true');&#xA;    tag.textContent = tag.textContent.replace(/^\/\/&lt;!\[CDATA\[/, '');&#xA;  })();&#xA;&lt;/script&gt;&#xA;</code></pre>&#xA;&#xA;<p>The important line is <code>'text/jsx;harmony=true'</code>. <a href="http://jsfiddle.net/ke4bmqny/2/" rel="noreferrer">Check the updated fiddle</a>.</p>&#xA;