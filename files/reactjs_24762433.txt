24762433
React renderComponent replace DOM
<p>JSX code:</p>&#xA;&#xA;<pre><code>var App = React.createClass({&#xA;  render: function() {&#xA;    return &lt;div className="darken"&gt;hello world&lt;/div&gt;&#xA;  }&#xA;});&#xA;React.renderComponent(&lt;App/&gt;, document.querySelector('.main'))&#xA;</code></pre>&#xA;&#xA;<p>HTML:</p>&#xA;&#xA;<pre><code>&lt;body&gt;&#xA;  &lt;header&gt;welcome&lt;/header&gt;&#xA;  &lt;div class="main"&gt;&lt;/div&gt;&#xA;&lt;/body&gt;&#xA;</code></pre>&#xA;&#xA;<p><code>React.renderComponent</code> will append rendered JSX to <code>&lt;div class="main"&gt;</code>. Output of HTML will be:</p>&#xA;&#xA;<pre><code>&lt;body&gt;&#xA;  &lt;header&gt;welcome&lt;/header&gt;&#xA;  &lt;div class="main"&gt;&#xA;    &lt;div class="darken"&gt;hello world&lt;/div&gt;&#xA;  &lt;/div&gt;&#xA;&lt;/body&gt;&#xA;</code></pre>&#xA;&#xA;<p>Is it possible <code>React.renderComponent</code> replace <code>&lt;div class="main"&gt;</code>, so what I expect like this:</p>&#xA;&#xA;<pre><code>&lt;body&gt;&#xA;  &lt;header&gt;welcome&lt;/header&gt;&#xA;  &lt;div class="darken"&gt;hello world&lt;/div&gt;&#xA;&lt;/body&gt;&#xA;</code></pre>&#xA;
<p>Sure, just use:</p>&#xA;&#xA;<pre><code>React.render(&lt;App/&gt;, document.body);&#xA;</code></pre>&#xA;
<p>Was having a problem getting my component to render in React 0.14.0. My solution was make sure your scripts have a special type:</p>&#xA;&#xA;<pre><code>&lt;script type="text/babel" src='js/app.js'&gt;&lt;/script&gt;&#xA;</code></pre>&#xA;&#xA;<p>Include these three libraries:</p>&#xA;&#xA;<pre><code>&lt;script src="https://cdnjs.cloudflare.com/ajax/libs/react/0.14.0/react.js"&gt;&lt;/script&gt;&#xA;  &lt;script src="https://cdnjs.cloudflare.com/ajax/libs/react/0.14.0/react-dom.js"&gt;&lt;/script&gt;&#xA;  &lt;script src="https://cdnjs.cloudflare.com/ajax/libs/babel-core/5.8.23/browser.min.js"&gt;&lt;/script&gt;&#xA;</code></pre>&#xA;&#xA;<p>Then use <code>ReactDOM.render()</code>:</p>&#xA;&#xA;<pre><code>ReactDOM.render(&lt;Timer start={Date.now()} /&gt;, document.getElementById('app'));&#xA;</code></pre>&#xA;&#xA;<p>I'm not even using ES6 or a build system. Simple solution to get text on the screen with react.js</p>&#xA;
<p>Had a similar problem and this is what I came up with.&#xA;Replace it with a simple span tag so it doesn't affect your styling. &#xA;</p>&#xA;&#xA;<pre><code>const appContainer = document.createElement('span')&#xA;const mainEl = document.querySelector('.main')&#xA;mainEl.parentNode.replaceChild(appContainer, mainEl)&#xA;React.renderComponent(&lt;App/&gt;, mainEl)&#xA;</code></pre>&#xA;&#xA;<p>Which will produce&#xA;</p>&#xA;&#xA;<pre><code>&lt;body&gt;&#xA;  &lt;header&gt;welcome&lt;/header&gt;&#xA;  &lt;span&gt; &lt;!-- this acts as the container for react --&gt;&#xA;    &lt;div class="darken"&gt;hello world&lt;/div&gt;&#xA;  &lt;/span&gt;&#xA;&lt;/body&gt;&#xA;</code></pre>&#xA;