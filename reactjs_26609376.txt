26609376
How can I disable animation in a reactjs component first time
<p>I have a component with a list of items. I want each time I add or remove an item from the list to have animation. Everything it's OK with that.</p>&#xA;&#xA;<p>My only problem is that I want to disable the animation first time when  the component initialize.</p>&#xA;&#xA;<p><a href="http://jsfiddle.net/kb3gN/6887/" rel="nofollow">http://jsfiddle.net/kb3gN/6887/</a></p>&#xA;&#xA;<pre><code>var ReactCSSTransitionGroup = React.addons.CSSTransitionGroup;&#xA;&#xA;var Hello = React.createClass({&#xA;    getInitialState: function () {&#xA;        return {items: []};&#xA;    },&#xA;    addColor:function(){&#xA;    this.setState({items:this.state.items.concat('new color')});&#xA;    },&#xA;    componentDidMount: function () {&#xA;        var items = ['blue', 'red', 'green'];&#xA;        this.setState({items: items});&#xA;    },&#xA;    render: function () {&#xA;        var contentList = this.state.items.map(function (i,k) {&#xA;            return (&lt;li&gt;{i}&lt;/li&gt;);&#xA;        });&#xA;        return (&#xA;                &lt;div&gt;&#xA;                Hello &lt;button onClick={this.addColor}&gt;add&lt;/button&gt;&#xA;&lt;ReactCSSTransitionGroup transitionName="example"&gt;&#xA;                {contentList}&#xA;                &lt;/ReactCSSTransitionGroup&gt;&#xA;&#xA;                &lt;/div&gt;&#xA;        )&#xA;    }&#xA;});&#xA;&#xA;React.render(&lt;Hello name="World" /&gt;, document.body);&#xA;</code></pre>&#xA;&#xA;<p>So, how can I make the animation appear only when changing the list and not ont the very first setState from didMount    method?</p>&#xA;
<p>Using <code>componentWillMount</code> instead of <code>componentDidMount</code> it will work perfectly as shown in this fiddle : <a href="http://jsfiddle.net/ghislaindj/jpuyd3gb/1/" rel="nofollow">http://jsfiddle.net/ghislaindj/jpuyd3gb/1/</a></p>&#xA;&#xA;<p>However, if your real code must fetch the data in DidMount, you should use ssorallen comment.</p>&#xA;