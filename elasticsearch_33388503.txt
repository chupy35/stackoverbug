33388503
Save ElasticSearch snapshot physical files in specific location
<p>To take a snap shot for ElasticSearch, we will need to first set up the snapshot repository:</p>&#xA;&#xA;<pre><code>$ curl -XPUT 'http://localhost:9200/_snapshot/repo_companyA' -d '{ &#xA;    "type" : "fs", &#xA;    "settings": {&#xA;        "location": "/data/backup/companyA", &#xA;        "compress":true&#xA;    }&#xA;}'&#xA;</code></pre>&#xA;&#xA;<p>If my ElasticSearch is running on a G: hard drive, then the physical snapshot would be stored inside G: drive folder of where the ElasticSearch is running on.</p>&#xA;&#xA;<p>What if I want my snapshot to be saved into F: drive instead?</p>&#xA;&#xA;<p>Is it possible to set the repository to save the copy of snapshot into a different, specific physical location?</p>&#xA;