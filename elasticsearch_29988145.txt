29988145
Elastic search, most efficient filter for boolean mapping, ie select all true/false
<p>I have the following field mapping:</p>&#xA;&#xA;<pre><code>mapping.add_property(BooleanField(name='my_bool'))&#xA;</code></pre>&#xA;&#xA;<p>I want to return all the items where this field is true/false and am currently doing this with a term filter.  Is there are more efficient way to structure this filter?</p>&#xA;&#xA;<p>Thanks.</p>&#xA;&#xA;<pre><code>{&#xA;    "query" : {&#xA;        "term" : { "my_bool" : "true"}&#xA;    }&#xA;}&#xA;</code></pre>&#xA;
<p>using a <a href="http://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-term-filter.html" rel="nofollow">term filter</a>  would be  more efficient in this </p>&#xA;&#xA;<p>1) a query involves calculating <a href="http://www.elastic.co/guide/en/elasticsearch/guide/master/relevance-intro.html" rel="nofollow">scores</a> for documents which is not relevant for the above case.</p>&#xA;&#xA;<p>2) a term filter would iterate over all documents building a bitset which is then cached for subsequent queries</p>&#xA;&#xA;<pre><code>{&#xA;     "filter" : {&#xA;          "term" : {"my_bool" : "true"}&#xA;      }&#xA;&#xA;}&#xA;</code></pre>&#xA;