34250016
Nest elasticsearch sort by enum values specific order
<p>product model in elasticSearch is like this:</p>&#xA;&#xA;<pre><code>public class Product&#xA;{&#xA;    public int ProductId { get; set; }&#xA;&#xA;    public string BrandName { get; set; }&#xA;&#xA;    public string Name { get; set; }&#xA;&#xA;    public ProductStatus ProductStatus { get; set; }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>and ProductStatus enum specify Status of product</p>&#xA;&#xA;<pre><code>public enum ProductStatus&#xA;{&#xA;    Available = 1,&#xA;    UnAvailable = 2,&#xA;    CommingSoon = 3,&#xA;    OutOfOrder = 4&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>i want sort product search result based on this order: Available, CommingSoon, UnAvailable and OutOfOrder, and may this order change later,&#xA;how this sort possible in elasticSearch, and NEST</p>&#xA;
<p>I think you can do function score to boost for each of the variable then do sum on function scores</p>&#xA;&#xA;<pre><code>function_score": {&#xA;    "query": {},&#xA;    "boost": "boost for the whole query",&#xA;    "functions": [&#xA;        {&#xA;            "filter": {},&#xA;            "FUNCTION": {}, &#xA;            "weight": number&#xA;        },&#xA;        {&#xA;            "FUNCTION": {} &#xA;        },&#xA;        {&#xA;            "filter": {},&#xA;            "weight": number&#xA;        }&#xA;    ],&#xA;    "max_boost": number,&#xA;    "score_mode": "(multiply|max|...)",&#xA;    "boost_mode": "(multiply|replace|...)",&#xA;    "min_score" : number&#xA;}&#xA;</code></pre>&#xA;