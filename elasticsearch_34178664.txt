34178664
Disable stemming for a exact phrase match
<p>I have stemming enabled on my analyzer with a standard tokenizer. How can I disable stemming for extact phrase matches?</p>&#xA;&#xA;<p>For example, I have the following query:</p>&#xA;&#xA;<pre><code>                "query_string" : {&#xA;                    "fields" : ["*content],&#xA;                    "default_operator": "AND", &#xA;                    "query" :"\"TRANSIENT ISCHAEMIC ATTACK\"",&#xA;                    "use_dis_max" : true,&#xA;                    "phrase_slop": 0&#xA;                }&#xA;</code></pre>&#xA;&#xA;<p>The problem currently is that it returns <code>TRANSIENT ISCHAEMIC ATTACKS</code> and <code>TRANSIENT ISCHAEMIC ATTACK</code>, I only want it to return <code>TRANSIENT ISCHAEMIC ATTACK</code> but I still want to retain the ability to search for root words for non exact phrase queries.</p>&#xA;