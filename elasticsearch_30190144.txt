30190144
Elasticsearch clustering configuration in node.js
<p>I have to setup elastic-search clustering for my node.js application. For testing i have 2 Virtual Machines(ubuntu) running , in that in 1st machine i modified the <code>elasticsearch.yml</code> as following</p>&#xA;&#xA;<pre><code>cluster.name: elasticsearch&#xA;node.name: "naodename1" , also ES running in port 9200&#xA;</code></pre>&#xA;&#xA;<p>and in the second machine as follows:</p>&#xA;&#xA;<pre><code>cluster.name: elasticsearch&#xA;node.name: "node_1" , ES running in port 9400&#xA;</code></pre>&#xA;&#xA;<p>I stopped machine 1 ES , now i connected from my node app, data is coming, Ithink from machine 2, so clustering works here.</p>&#xA;&#xA;<p>But when i create an index in machine1 that should replicate to machine2 ES.&#xA; I created a sample index in machine1 as follows:</p>&#xA;&#xA;<pre><code>curl -XPUT 'http://localhost:9400/twitter/tweet/1' -d '{&#xA;"user" : "kimchy",&#xA;"post_date" : "2009-11-15T14:12:12",&#xA;"message" : "trying out Elasticsearch"&#xA;}'&#xA;</code></pre>&#xA;&#xA;<p>Now i tried to search using machine1 ES as follows:</p>&#xA;&#xA;<pre><code>http://192.168.1.109:9200/twitter/_search?&amp;q=*&#xA;</code></pre>&#xA;&#xA;<p>I am receiving error:</p>&#xA;&#xA;<pre><code>"Index missing exception". Whether i am missing any configuration. Please share your ideas. Thanks in advance.&#xA;</code></pre>&#xA;