28862029
React: passed element doesn't instantiate
<p>Basic ReactJS question. I have the following React components:</p>&#xA;&#xA;<pre><code>var TopLevelElement = React.createClass({&#xA;    render: function () {&#xA;        return (&#xA;            &lt;Grouper top={Top} bottom={Bottom} /&gt;&#xA;        );&#xA;    }&#xA;});&#xA;&#xA;var Grouper = React.createClass({&#xA;    render: function () {&#xA;        return (&#xA;            &lt;div&gt;&#xA;                &lt;div className="top"&gt;{this.props.top}&lt;/div&gt;&#xA;                &lt;div className="bottom"&gt;{this.props.bottom}&lt;/div&gt;&#xA;            &lt;/div&gt;&#xA;        );&#xA;    }&#xA;});&#xA;</code></pre>&#xA;&#xA;<p><code>Top</code> and <code>Bottom</code> are React components that just have a render() that outputs some markup. </p>&#xA;&#xA;<p>When I do a <code>React.render(&lt;TopLevelElement /&gt;, el);</code> it only outputs the markup in Grouper, not in <code>Top</code> or <code>Bottom</code>. I've confirmed that those elements do work fine independently.</p>&#xA;&#xA;<p>Looks like the children aren't being instantiated at all. Any idea what I'm doing wrong?</p>&#xA;&#xA;<p>Thanks!</p>&#xA;
<p>Try this instead:</p>&#xA;&#xA;<pre><code>var TopLevelElement = React.createClass({&#xA;    render: function () {&#xA;        return (&#xA;            &lt;Grouper top={&lt;Top /&gt;} bottom={&lt;Bottom /&gt;} /&gt;&#xA;        );&#xA;    }&#xA;});&#xA;</code></pre>&#xA;