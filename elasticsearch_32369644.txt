32369644
Using elasticsearch dsl query with multiple indices
<p>I run a query with myindex(dota2) as you see below:&#xA;<a href="https://i.stack.imgur.com/xYj5N.png" rel="nofollow noreferrer"><img src="https://i.stack.imgur.com/xYj5N.png" alt="enter image description here"></a></p>&#xA;&#xA;<p>But I need indices is inside the query, so I include it as the docs in elasticsearch.co in my dsl query, and run it but all the indices return(Ijust want the make query just in two indices 'dota2','dota3') here:&#xA;<a href="https://i.stack.imgur.com/GFMUd.png" rel="nofollow noreferrer"><img src="https://i.stack.imgur.com/GFMUd.png" alt="enter image description here"></a></p>&#xA;&#xA;<p>What do I wrong here?</p>&#xA;
<p>What happens in your case is that when you use the <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-indices-query.html" rel="nofollow"><code>indices</code> query</a>, you also need to specify the <code>no_match_query</code> part to <code>none</code> otherwise <code>all</code> documents from the non-matching indices will be returned.</p>&#xA;&#xA;<p>However, you could also simply write your URL as follows:</p>&#xA;&#xA;<pre><code>GET dota2,dota3/_search&#xA;</code></pre>&#xA;