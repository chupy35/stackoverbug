29173039
Elastic regexp filter on more than one field
<p>the following example creates a filtered alias for any document in which the <strong>title</strong> field contains "smith".</p>&#xA;&#xA;<p>How can you create an alias that is filtered for a second field such as <strong>title OR description</strong>?</p>&#xA;&#xA;<p>["title","description"] does not work as I expected it to and I can not use the _all as it gives the incorrect results.</p>&#xA;&#xA;<pre><code>curl -XPOST 'http://localhost:9200/_aliases' -d '{&#xA;    "actions" : [&#xA;        {&#xA;            "add" : {&#xA;                 "index" : "myindex",&#xA;                 "alias" : "smithbooks",&#xA;                 "filter" : { "regexp" : { "title" : "smith" } } &#xA;            }&#xA;        }&#xA;    ]&#xA;}'&#xA;</code></pre>&#xA;
<p>I solved this using an OR filter which can be found here:</p>&#xA;&#xA;<p><a href="http://www.elastic.co/guide/en/elasticsearch/reference/master/query-dsl-or-filter.html" rel="nofollow">http://www.elastic.co/guide/en/elasticsearch/reference/master/query-dsl-or-filter.html</a></p>&#xA;