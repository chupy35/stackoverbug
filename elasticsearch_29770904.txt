29770904
Export elasticsearch result into csv
<p>I am trying to export Elasitcsearch result into CSV format in <code>Elasticsearch 1.3.0</code>. I found two suggestion for that. But these solutions aren't working for me. </p>&#xA;&#xA;<ul>&#xA;<li>Using <code>head plugin</code> structure query option but its limitation to export 25k records only.</li>&#xA;<li>Using <a href="https://github.com/jprante/elasticsearch-csv" rel="nofollow">Elasticsearch-CSV</a> plugin , Unfortunately I am unable to find it for <code>Elasticsearch-1.3.0</code>. This plugin jar available for <code>1.4.0</code> version.</li>&#xA;</ul>&#xA;&#xA;<p>Any other Suggestions?</p>&#xA;
<p>To keep using tools from the Elasticsearch stack (named <strong>ELK</strong>), &#xA;you could use an ETL named <a href="http://logstash.net/" rel="nofollow">Logstash</a> (the <code>L</code> from <strong>ELK</strong>).</p>&#xA;&#xA;<p>Create a configuration based on following :</p>&#xA;&#xA;<ul>&#xA;<li>input : <a href="http://logstash.net/docs/1.4.2/inputs/elasticsearch" rel="nofollow">elasticsearch</a></li>&#xA;<li>output : <a href="http://logstash.net/docs/1.4.2/outputs/csv" rel="nofollow">csv</a></li>&#xA;</ul>&#xA;&#xA;<p>Besides this, it's also a <a href="https://github.com/elastic/kibana/issues/1992" rel="nofollow">very requested feature</a> for Kibana 4 (the <code>K</code> from <strong>ELK</strong> stack, visualization webapp based on ElasticSearch cluster), which should be implemented in near future.</p>&#xA;