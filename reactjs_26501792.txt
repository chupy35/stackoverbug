26501792
Rid of repeated spans in React.js?
<p>React renders multiple spans, only with the actual value. Any advice on how to fix this: </p>&#xA;&#xA;<p><img src="https://i.stack.imgur.com/qIOzj.png" alt="enter image description here"></p>&#xA;&#xA;<p>And if it matter, the name of the user isn't suppose to be in a span, but as innerHTML of ".chatUser"</p>&#xA;&#xA;<p>The actual render method is short: </p>&#xA;&#xA;<pre><code>createShortUsername: function() {&#xA; // shortName is first two letter of first name. &#xA; var shortName = this.props.userName.split(" ")[0].slice(0, 2);&#xA; console.log(shortName);&#xA; return shortName;&#xA;},&#xA;render: function() {&#xA; return (&#xA;   &lt;div className="chatUser"&gt; {this.createShortUsername()} &lt;/div&gt;&#xA; );&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>Thanks!</p>&#xA;
<p>As suggested by @ivarni, the extra spans were caused by white spaces!</p>&#xA;&#xA;<p>To prevent this write your code without white spaces around the braced expressions. </p>&#xA;&#xA;<pre><code>&lt;ReactElement&gt;{noRoomForWhiteSpace}&lt;/ReactElement&gt;&#xA;</code></pre>&#xA;
<p>Its actually some special characters(non visible in your editor) that creeps into the code.(Likely if you had copy-pasted). For me it was <code>\u2028</code>. Check the generated javascript file to see what extra characters got into the code. &#xA;To get rid of it, you could remove the line that caused it and type it again( as opposed to copying and pasting)</p>&#xA;