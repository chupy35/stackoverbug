30861212
duplicates of documents getting created in jdbc river index (elastic search)
<p>I'm pulling data from my database whose table name is <strong>Categories</strong> which has columns <strong>Cat_id</strong> and <strong>Cat_name</strong>. I currently have 6 rows as my test data and started river instance with the following configuration with POSTMAN HTTP client - </p>&#xA;&#xA;<pre><code>     http://localhost:9200/_river/test/_meta &#xA;       {&#xA;                "type" : "jdbc",&#xA;                "jdbc" : {&#xA;                        "url" : "jdbc:mysql://localhost:3306/elastic",&#xA;                        "user" : "root",&#xA;                        "password" : "password",&#xA;                        "sql" : "select Cat_id from Categories",&#xA;                        "index" : "sample",&#xA;                        "schedule" : "0/10 * * ? * *",&#xA;                        "autocommit" : true&#xA;&#xA;                 }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>The problem is, as I've scheduled for 10 seconds, query runs for every 10 seconds and duplicates of data is getting created in the index. That is, &#xA;if my test data is - </p>&#xA;&#xA;<pre><code>       Cat_id      Cat_name&#xA;         1          Books&#xA;         2          Mobiles&#xA;         3          Laptops&#xA;</code></pre>&#xA;&#xA;<p>My resulting index would get updated for every 10 seconds and same data gets added for every 10 seconds. That is, copies of data would be created.</p>&#xA;&#xA;<p>I want only changes of data to be added to my index while no duplicates are created for my existing data ( Ex: Books,Mobiles,Laptops ).</p>&#xA;