34133840
Adavancedquery in Elastic search
<p>I am working on Elastic Search for my current Project. I am working Keyword Word Based functionality. And My Query as follows</p>&#xA;&#xA;<pre><code>$query = array("from" =&gt; 0,&#xA;            "size" =&gt; 20,&#xA;            "query" =&gt; array(&#xA;                "filtered" =&gt; array(&#xA;                    "query" =&gt; array("match" =&gt; array('_all' =&gt; trim(strtolower($keyword))))&#xA;        )));&#xA;        $result = $this-&gt;elasticsearch-&gt;advancedquery("services", json_encode($query));&#xA;</code></pre>&#xA;&#xA;<p>if user enters his keyword it comes to $keyword and search "services" type. this functionality working fine. but I want search that keyword in multiple types. i.e. &#xA;In My Index.. I am having two types "products" and "services". &#xA;I need to search my keyword in both types.</p>&#xA;
<p>use multi_match</p>&#xA;&#xA;<pre><code> {"query" : {"multi_match" : {"query": $keyword,"fields": [ "products", "services" ]}}}&#xA;</code></pre>&#xA;