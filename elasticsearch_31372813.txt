31372813
elasticsearch sub-aggregation on different index
<p>I have two indexes, <code>tweets</code> and <code>hashtags</code> </p>&#xA;&#xA;<p>I'm performing an aggregation on <code>tweets</code> to surface the most frequently used hashtags. Straightforward.</p>&#xA;&#xA;<p>Within <code>hashtags</code> i am storing a list of hashtags, each with a property representing a "favorited" count.</p>&#xA;&#xA;<p>I want to perform the aggregation on <code>tweets</code>, and then for the returned list of hashtags, look them up in <code>hashtags</code> index and retrieve <code>favorite_count</code>. I'd like to do this all within the same query.</p>&#xA;&#xA;<p>I'm willing to store <code>hashtags</code> and <code>favorite_count</code> differently if it gets the job done. </p>&#xA;