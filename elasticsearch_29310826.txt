29310826
Broken random scoring with elastic search when specific fields are used
<p>I am encountering a weird problem with random scoring in elastic and it appears only when fields are used:</p>&#xA;&#xA;<pre><code>{&#xA;    "from" : 0, "size" : 50,&#xA;    "fields":   { "fields": [ "id", "name"] },&#xA;    "query": {&#xA;        "function_score": {&#xA;            "query": {&#xA;                "bool": { "must_not": { "term": { "disabled": true}}}&#xA;                },&#xA;            "random_score": {&#xA;                    "seed": 123456&#xA;            }&#xA;        }&#xA;    }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>Then this construct is used, _score of every document is 1!&#xA;Removing "fields" starts popping up some items with top one having _score of 16775988. I guess I do not understand the interaction between fields and scoring, how does one affect the other?</p>&#xA;&#xA;<p>Thanks for your help!</p>&#xA;