29887456
how elasticsearch exclude a specific value?
<p>A filed named as TenantId and it is value is in TenantId[*] format. &#xA;I want to exclude the value like TenanId[] from query result.</p>&#xA;&#xA;<p>my query string is:</p>&#xA;&#xA;<pre><code>    {&#xA;       "query": {&#xA;          "filtered": {&#xA;             "query": {&#xA;                "match": {&#xA;                  "type": "sld"&#xA;               }&#xA;             },&#xA;             "filter": {&#xA;                "not": {&#xA;                   "term": {&#xA;                      "TenantId": "TenantId[]"&#xA;                   }&#xA;                }&#xA;             }&#xA;          }&#xA;       }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>but the result was still include the "TenantId[]".&#xA;Is there any way to do this?</p>&#xA;