34152252
Elasticsearch integer range query is not working
<p>I have field hcc_member_id as of Integer type. I want to perform range query on this field. I tried queries given in the ES documentation, but it does not seem to work. No matter what the query is it always returns same response.</p>&#xA;&#xA;<p>I think I am doing things in a wrong way but not able to identify the problem. Any help is good. </p>&#xA;&#xA;<p><a href="https://i.stack.imgur.com/pjAtj.png" rel="nofollow noreferrer"><img src="https://i.stack.imgur.com/pjAtj.png" alt="enter image description here"></a></p>&#xA;
<p>this is a working (for me) query </p>&#xA;&#xA;<p>//EDIT // <strong>IT WORK ONLY IN POST NOT GET</strong></p>&#xA;&#xA;<pre><code>{&#xA;    "query" : {&#xA;        "range" : {&#xA;            "hcc_member_id" : {&#xA;                "gte" : 1000&#xA;            }&#xA;        }&#xA;    }&#xA;}&#xA;</code></pre>&#xA;
<p>You should use POST instead of GET. Otherwise your Json will be ignored.</p>&#xA;&#xA;<p>Furtermore you should add a "query" field to our json:&#xA;(without query you will get something like <code>No parser for element [range]]</code>)</p>&#xA;&#xA;<pre><code>{&#xA;  "query": {&#xA;    "range": {&#xA;      "hc_member_id": {&#xA;        "gte": 1000&#xA;      }&#xA;    }&#xA;  }&#xA;}&#xA;</code></pre>&#xA;