33073352
elasticsearch: how to measure index size on a cluster? (true confusion!)
<p>Elasticsearch 1.7.x on centos&#xA;3 node cluster&#xA;Config is essentially stock (out of box)</p>&#xA;&#xA;<p>We monitor these elements from _stats</p>&#xA;&#xA;<pre><code>index.size: 94,594,769,960 bytes&#xA;index.primary_size: 67,570,017,426 bytes&#xA;</code></pre>&#xA;&#xA;<p>But when we look at the OS, df -h, on any one node, it says:</p>&#xA;&#xA;<pre><code>Filesystem      Size  Used Avail Use% Mounted on&#xA;/dev/xvda1      158G   48G  103G  32% /&#xA;</code></pre>&#xA;&#xA;<p>So.... where are the 94 gig of indexes in index.size?</p>&#xA;&#xA;<p>And, this is a one-index env. Why is there a delta between index.size and index.primary_size?</p>&#xA;&#xA;<p>Essentially: How do I map the stats I see in ES to what is consumed from the OS?</p>&#xA;