31985409
how to search ElasticSearch by inner objects
<p>consider the following index of <code>ES</code></p>&#xA;&#xA;<pre><code>{&#xA;    "hits": {&#xA;        "total": 5,&#xA;        "max_score": 1,&#xA;        "hits": [&#xA;            {&#xA;                "_source": {&#xA;                    "name": "A",&#xA;                    "postalCode": {&#xA;                        "code": "postcodeA",&#xA;                        "validated": false&#xA;                    }&#xA;                }&#xA;            },&#xA;            {&#xA;                "_source": {&#xA;                    "name": "B",&#xA;                    "postalCode": {&#xA;                        "code": "postcodeB",&#xA;                        "validated": true&#xA;                    }&#xA;                }&#xA;            }&#xA;        ]&#xA;    }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>how do I search for hits with condition:</p>&#xA;&#xA;<pre><code>_source.postalCode.validated=true&#xA;</code></pre>&#xA;&#xA;<p>I'm still new and trying to get used to ES.&#xA;some of the queries that I've tried are the followings:</p>&#xA;&#xA;<pre><code>q=_source.pastalCode.validated=true&#xA;q=_source.postalCode.validated&#xA;q=_source:postalCode:validated&#xA;</code></pre>&#xA;
<p>You can either perform a DSL query like so:</p>&#xA;&#xA;<pre><code>{&#xA;    "query": {&#xA;        "filtered": {&#xA;             "filter": {&#xA;                 "term": {&#xA;                     "postalCode.validated": "true"&#xA;                 }&#xA;             }&#xA;        }&#xA;    }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>Or using the querystring, as you tried above:</p>&#xA;&#xA;<p><code>q=postalCode.validated: true</code></p>&#xA;&#xA;<p>You do not need to include the <code>_source</code> part.</p>&#xA;