34066015
NoNodeAvailableException: None of the configured nodes are available exception occurring intermittently: what is the exact cause?
<p>Need to finalize the exact root cause of "None of the configured nodes are available exception" occuring in elasticsearch.</p>&#xA;&#xA;<p>The following are the list of possible causes found by searching available articles:<br/>&#xA;1.  Incorrect host/port/cluster name<br/>&#xA;2.  Incorrect version of log4j in application<br/>&#xA;3.  Mismatching version of elasticsearch JAR and Java/Transport client used<br/>&#xA;4.  GC taking more cycles and subsequently killing TCP connections<br/>&#xA;5.  Failure/disconnected/down Load balancer node<br/>&#xA;6. client.close() method is invoked before performing any operation Index/Search on client<br/>&#xA;7. Setting <code>client.transport.sniff=true</code> </p>&#xA;&#xA;<pre><code>sniffOnConnectionFault=true&#xA;</code></pre>&#xA;&#xA;<p>property to true shall avoid this<br/>&#xA;8.  TCP connection settings in elasticsearch</p>&#xA;&#xA;<pre><code>net.ipv4.tcp_keepalive_time=600 &#xA;net.ipv4.tcp_keepalive_intvl=60 &#xA;net.ipv4.tcp_keepalive_probes=3&#xA;</code></pre>&#xA;&#xA;<p>Configuration Details:&#xA;Elasticsearch version - 1.5.1&#xA;Client referred from elasticsearch-1.5.1.jar <br/></p>&#xA;&#xA;<p>elasticsearch.yml properties(externalized to properties file in web application)<br/></p>&#xA;&#xA;<pre><code>elasticsearch.cluster.sniff=true&#xA;elasticsearch.cluster.enabled=true&#xA;elasticsearch.transport.timeout=60000&#xA;elasticsearch.search.timeout=60000&#xA;&#xA;elasticsearch.status.yellow.timeout=10000&#xA;elasticsearch.index.ack.timeout=60000&#xA;elasticsearch.index.attachment.ack.timeout=45000&#xA;</code></pre>&#xA;&#xA;<p>I have been verifying all the findings above on my application. But I need to pinpoint a specific condition or point as the exact root cause of this issue? Suggestions or Ideas to finalize on exact root cause.</p>&#xA;