33788602
Elasticsearch returned fields renaming
<p>In Elasticsearch index , I have field called <code>category</code> , and I want to rename it to <code>cat</code> in the returned array of objects in stead of array of actual value , something like MySQL <code>SELECT category as cat</code> </p>&#xA;&#xA;<p>I tried to use <a href="https://www.elastic.co/guide/en/elasticsearch/reference/1.4/search-request-fields.html#partial" rel="nofollow">partial_fields</a> &#xA;, it returns an array </p>&#xA;&#xA;<pre><code>"partial_fields" : {&#xA;   "cat" : {&#xA;       "include" : ["category"]&#xA;   }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>but it returns </p>&#xA;&#xA;<pre><code>"fields": {&#xA;   "cat": [&#xA;      {&#xA;        "category": 1&#xA;      }&#xA;   ]&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>in fact I want it to be something like </p>&#xA;&#xA;<pre><code>"fields": {&#xA;   "cat": [1]&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>is there any way to do this ?</p>&#xA;
<p>That's not possible, unfortunately. You'll have to handle this in your application.</p>&#xA;