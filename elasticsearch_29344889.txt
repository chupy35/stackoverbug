29344889
`filter` is incompatible between v0.90 and v1.4?
<p>In Elasticsearch version "1.4.2", parameter below works well.</p>&#xA;&#xA;<pre class="lang-json prettyprint-override"><code>{"filter":{"terms":{"foo":["xxx"]}}}&#xA;</code></pre>&#xA;&#xA;<p>But in version "0.90.0", the same parameter doesn't work. (hits 0)</p>&#xA;&#xA;<p>I initially suspected that I have set wrong <code>mapping</code>, but it was the same.</p>&#xA;&#xA;<pre class="lang-json prettyprint-override"><code>{&#xA;  "my_type" : {&#xA;    "properties" : {&#xA;      "foo" : {&#xA;        "type" : "string"&#xA;      }&#xA;    },&#xA;    ...&#xA;  }&#xA;}&#xA;</code></pre>&#xA;&#xA;<p>The only difference between these two servers seems to be their versions.</p>&#xA;&#xA;<p>Is it possible <code>filter</code> is incompatible between v0.90 and v1.4?</p>&#xA;&#xA;<p>Or any possibility of my mistakes.</p>&#xA;